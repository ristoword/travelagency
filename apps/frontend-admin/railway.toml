[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/frontend-admin/Dockerfile"

[deploy]
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
